import 'package:flutter/material.dart';

class ObatDanCairan extends StatelessWidget {
  const ObatDanCairan({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Obat dan Cairan")),
      body: Center(child: const Text("Obat dan Cairan")),
    );
  }
}