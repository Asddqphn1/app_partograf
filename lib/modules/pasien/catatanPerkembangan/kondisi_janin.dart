import 'package:flutter/material.dart';

class KondisiJanin extends StatelessWidget {
  const KondisiJanin({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Kondisi Janin")),
      body: Center(child: const Text("Kondisi Janin")),
    );
  }
}
