import 'package:flutter/material.dart';

class KemajuanPersalinan extends StatelessWidget {
  const KemajuanPersalinan({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Kemajuan Persalinan")),
      body: Center(child: Text("Kemajuan Persalinan")),
    );
  }
}
